//sum of elements
#include<stdio.h>
int main()
{
	int n,i,j,sum=0;
	printf("enter the size");
	scanf("%d",&n);
	int a[n];
	for(i=0;i<=n;i++)
	{
	scanf("%d",&a[i]);
	sum=sum+a[i];
	}
	printf("sum of digits=%d",sum);
}
